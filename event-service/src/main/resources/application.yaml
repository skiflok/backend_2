spring:
  application:
    name: event-service

  kafka:
    bootstrap-servers: localhost:29092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer

  output:
    ansi:
      enabled: always

server:
  port: 9997


logging:
  level:
    liquibase: info
    com.edu.eventservice: debug
