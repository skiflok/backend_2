spring:
  application:
    name: event-service

  kafka:
    bootstrap-servers: localhost:29092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
  #      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

  output:
    ansi:
      enabled: always

server:
  port: 9997

kafka:
  out:
    product:
      updates-stocks:
        topic: product-updates-stocks
      updates-price:
        topic: product-updates-price
  in:
    product:
      update-event:
        topic: product-update-event
        group-id: product-update-event-group

rest:
  client:
    shop-service:
      product:
        all:
          url: ${product-all-url:/api/v1/product/all}

scheduler:
  product:
    updates-stocks: 10000
    updates-price: 20000


logging:
  level:
    liquibase: info
    com.edu.eventservice: debug
